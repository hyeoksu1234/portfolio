{
  "name": "portfolio_website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-site.js",
    "check": "npm run build && node scripts/check-site.js",
    "dev": "python3 -m http.server 5173 || python -m http.server 5173",
    "start": "npm run dev"
  }
}
